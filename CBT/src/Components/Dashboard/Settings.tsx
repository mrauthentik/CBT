import SideBar from "../SideBar"

const Settings = () => {
  return (
    <div>

        <SideBar />
    <div className='settings'>
      <h1>Settings</h1>
    </div>
    </div>
  )
}

export default Settings
